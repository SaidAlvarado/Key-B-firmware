config ZMK_BLE
	default y

config ZMK_USB
	default y

if SHIELD_KEYB

config ZMK_KEYBOARD_NAME
    default "KeyB"

if USB_DEVICE_STACK

config USB_NRFX
    default y

endif # USB_DEVICE_STACK

config BT_CTLR
    default BT

endif